{% import "_includes/forms" as forms %}
{% set layoutId = craft.request.getPost('layoutId') %}
{% set layout = craft.formBuilder2.getLayout(layoutId) %}

{{ forms.textField({
	label: "Template Name"|t,
	instructions: "Place your templates into templates/layouts/templates."|t,
	id: 'templatePath',
	class: 'templatePath',
	value: (layout.templateName is defined ? layout.templateName : ''),
	size: 60
}) }}
