<section class="cmp-tout section-collapsible fields-section">
    <header class="theme-{{ theme is defined ? theme }}">
        <div class="titlebar">
            <span class="tout-title">{{ "Fields" |t }}</span>
            <div class="preview"></div>
        </div>
        <div class="actions">
            <a class="icons collapser last" title="Collapse" role="combobox" tabindex="0"></a>
            <div class="menu">
                <ul class="padded">
                    <li><a data-icon="collapse" data-action="collapse">{{ "Collapse"|t }}</a></li>
                    <li class="hidden"><a data-icon="expand" data-action="expand">{{ "Expand"|t }}</a></li>
                </ul>
            </div>
        </div>
    </header>

    <div class="body theme-{{ theme is defined ? theme }}">
        {% include 'formbuilder2/forms/_includes/fields/fields' ignore missing %}
    </div>
</section>