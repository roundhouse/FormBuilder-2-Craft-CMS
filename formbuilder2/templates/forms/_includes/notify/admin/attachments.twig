{% set container = {
    title: 'Attachments' |t,
    info: null,
    icon: null,
    class: null,
    id: null
} %}

{% set inputs = {
    checkbox: {
        type: 'checkbox',
        name: 'notify[admin][attachments][enabled]',
        value: 'true',
        checked: form.notify.admin.attachments.enabled is defined and form.notify.admin.attachments.enabled == 'true' ? true : false,
        class: 'hidden',
        id: null
    }
} %}

{% set modal = null %}

{{ components.optionItem(
    container,
    modal,
    inputs
) }}